@model IEnumerable<Cover>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary text-white">Category List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Covers" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus"></i> &nbsp; Create New Cover
            </a>
        </div>
    </div>
    <br>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>
                Cover Name
            </th>
            <th>
                Delete?
            </th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach(var cover in Model)
            {
                <tr>
                    <td width="50%">
                            <a asp-controller="Covers" asp-action="Edit" asp-route-id="@cover.Id">@cover.Name</a>
                    </td>
                    <td>
                            <form action="Covers/Delete/@cover.Id" method="post">
                    @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-danger" onclick="return confirm(&quot;Are you sure you want to delete this record? &quot;"><i class="bi bi-trash"></i>Delete?</button>
                    </form>
                        </td>
                </tr>
            }
        }
    </tbody>
</table>
</div>
